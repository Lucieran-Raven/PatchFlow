apiVersion: apps/v1
kind: Deployment
metadata:
  name: patchflow-frontend
  namespace: patchflow
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: frontend
        resources:
          requests:
            memory: "256Mi"
            cpu: "125m"
          limits:
            memory: "512Mi"
            cpu: "250m"
