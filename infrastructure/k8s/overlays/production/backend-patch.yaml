apiVersion: apps/v1
kind: Deployment
metadata:
  name: patchflow-backend
  namespace: patchflow
spec:
  replicas: 5
  template:
    spec:
      containers:
      - name: backend
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
