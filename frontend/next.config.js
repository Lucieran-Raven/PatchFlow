/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Enable if needed for external image domains
    // images: {
    //   unoptimized: true
    // }
  }
}

module.exports = nextConfig
