import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "PatchFlow - Security That Fixes Itself",
  description: "Autonomous AI security engineer that finds vulnerabilities, generates fixes, and deploys them automatically. Reduce security debt by 75% in 24 hours.",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
